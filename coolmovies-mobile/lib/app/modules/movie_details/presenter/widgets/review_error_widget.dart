import 'package:coolmovies/app/commons/utils/exports.dart';

class ReviewErrorWidget extends StatelessWidget {
  const ReviewErrorWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
