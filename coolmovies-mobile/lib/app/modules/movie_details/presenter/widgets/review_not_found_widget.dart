import 'package:coolmovies/app/commons/utils/exports.dart';

class ReviewNotFoundWidget extends StatelessWidget {
  const ReviewNotFoundWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
