import 'package:coolmovies/app/commons/utils/exports.dart';

class MovieNotFoundWidget extends StatelessWidget {
  const MovieNotFoundWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
